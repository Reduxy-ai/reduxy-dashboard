# reduxy-dashboard

Next.js frontend dashboard for reduxy.ai

## Development
1. Install dependencies: `npm install`
2. Run development server: `npm run dev`
3. Open [http://localhost:3000](http://localhost:3000)
